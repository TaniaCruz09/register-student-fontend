export interface Student {
  id: string
  name: string
  grade: string
  year: number
  shift: string
}

export interface StudentAddBody {
  name: string
  grade: string
  year: number
  shift: string
}
